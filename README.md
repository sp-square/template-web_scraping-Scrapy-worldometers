# template-web_scraping-Scrapy-worldometers

To execute a spider from the cli, be in the same root as the `scrapy.cfg` file<br>
To execute the 'countries' spider for example, type: `scrapy crawl countries`
